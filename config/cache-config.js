/**
 * Конфигурация кэширования для Tennis Betting AI
 * Дифференцированные TTL для разных типов данных
 */

module.exports = {
  TTL: {
    // Текущие коэффициенты (быстро меняются)
    ODDS_LIVE: 2 * 60 * 60 * 1000, // 2 часа
    
    // Исторические коэффициенты (меняются реже)
    ODDS_HISTORICAL: 24 * 60 * 60 * 1000, // 24 часа
    
    // Список матчей (обновляется несколько раз в день)
    MATCH_LIST: 6 * 60 * 60 * 1000, // 6 часов
    
    // Информация о турнирах (меняется редко)
    TOURNAMENT_INFO: 7 * 24 * 60 * 60 * 1000, // 7 дней
    
    // Статистика игроков (меняется сезонно)
    PLAYER_STATS: 30 * 24 * 60 * 60 * 1000, // 30 дней
    
    // Конфигурации системы (практически не меняются)
    SYSTEM_CONFIG: 365 * 24 * 60 * 60 * 1000 // 1 год
  },
  
  // Настройки очистки кэша
  CLEANUP: {
    INTERVAL: 60 * 60 * 1000, // Чистка раз в час
    BATCH_SIZE: 100, // Максимум записей для очистки за раз
    MAX_AGE_DAYS: 30 // Максимальный возраст записей (дни)
  },
  
  // Ограничения размера
  LIMITS: {
    MAX_CACHE_SIZE_MB: 50, // Максимальный размер кэша
    MAX_ENTRIES: 1000, // Максимальное количество записей
    WARNING_THRESHOLD_MB: 40 // Порог предупреждения
  },
  
  // Типы ключей для автоматического определения TTL
  KEY_PATTERNS: {
    ODDS_LIVE: [/^odds_/, /^live_/, /^today_/],
    ODDS_HISTORICAL: [/^historical_/, /^closing_/, /^archive_/],
    MATCH_LIST: [/^matches_/, /^events_/, /^schedule_/],
    TOURNAMENT_INFO: [/^tournament_/, /^atp_/, /^wta_/, /^grandslam_/],
    PLAYER_STATS: [/^player_/, /^stats_/, /^rating_/],
    SYSTEM_CONFIG: [/^config_/, /^settings_/, /^bookmakers_/]
  },
  
  // Фолбэк TTL если не удалось определить тип
  DEFAULT_TTL: 2 * 60 * 60 * 1000, // 2 часа по умолчанию
  
  // Настройки производительности
  PERFORMANCE: {
    AUTO_PRUNING: true, // Автоматическая очистка старых записей
    COMPRESSION: false, // Сжатие данных (пока выключено)
    VALIDATION: true // Валидация данных при загрузке
  }
};